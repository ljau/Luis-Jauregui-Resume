import React from "react";
import { SideBarContainer, CustomLabel } from "..";

export const SideBar = () => {
  return (
    <SideBarContainer>
      <CustomLabel>Hello from side bar</CustomLabel>
    </SideBarContainer>
  );
};
